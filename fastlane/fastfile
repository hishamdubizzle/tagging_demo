import_from_git(
  url: "git@git.naspersclassifieds.com:olx/dubizzle/developers/mobile/mobile_cicd.git", # The URL of the repository to import the Fastfile from.
  branch: "HEAD", # The branch to checkout on the repository. Defaults to `HEAD`.
  path: "ios/Fastlane/tagging/fastfile" # The path of the Fastfile in the repository. Defaults to `fastlane/Fastfile`.
)